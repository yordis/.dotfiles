version: '3'

tasks:
  help:
    silent: true
    cmds:
      - $DOTFILES_ROOT/roles/zsh/src/bin/ubi --list-all

  dotfiles-docker-dir:
    silent: true
    cmds:
      - cd "$DOTFILES_ROOT/roles/docker/src"

  dotfiles-edit:
    silent: true
    cmds:
      - code "$DOTFILES_ROOT"
